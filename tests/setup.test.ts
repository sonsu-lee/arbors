import { describe, it, expect } from "vitest";

describe("Project Setup", () => {
  it("should have a working test environment", () => {
    // Given: the vitest test runner
    // When: a basic assertion is made
    // Then: it should pass
    expect(true).toBe(true);
  });
});
